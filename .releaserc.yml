repositoryUrl: "http://gitlab.3kles.local/3kles/lib/corejs/3kles-mongodb.git"

branches:
  - main
  - name: alpha
    prerelease: alpha
    channel: alpha
  - name: hotfix/**
    prerelease: true
  - name: "release/2.x"
    range: "2.x"
    prerelease: false
  - name: "release/3.x"
    range: "3.x"
    prerelease: false

plugins:
  - "@semantic-release/commit-analyzer"
  - "@semantic-release/release-notes-generator"
  - "@semantic-release/changelog"
  - - "@semantic-release/npm"
    - npmPublish: false # Ne publie pas sur npm
      pkgRoot: . # Dossier racine du package
  - - "@semantic-release/git"
    - assets:
        - package.json
        - package-lock.json
        - CHANGELOG.md
      message: |
        chore(release): v${nextRelease.version}

        🔖 Version: ${nextRelease.version}
        📦 Release type: ${nextRelease.type}

        📝 Notes de version:
        ${nextRelease.notes}
  - "@semantic-release/gitlab"